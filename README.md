# Фитнес-центр — Система управления
Веб-приложение для управления фитнес-центром с полным функционалом для клиентов и администраторов.

## Содержание

- [Описание проекта](#-описание-проекта)
- [Основные возможности](#-основные-возможности)
- [Технологический стек](#-технологический-стек)
- [Структура проекта](#-структура-проекта)
- [Установка и настройка](#-установка-и-настройка)
- [Использование](#-использование)
- [База данных](#-база-данных)
- [API](#-api)
- [Безопасность](#-безопасность)
- [Разработка](#-разработка)

## Описание проекта

Информационная система для управления фитнес-центром, которая позволяет:

- **Клиентам**: регистрироваться, просматривать услуги и тренеров, выбирать абонементы, оформлять заказы и отслеживать историю покупок
- **Администраторам**: управлять пользователями, услугами, тренерами, абонементами, заказами и расписанием

Система реализует полный цикл работы фитнес-центра: от выбора услуг до оплаты и управления активными абонементами.

## Основные возможности

### Для пользователей

- ✅ Регистрация и авторизация
- ✅ Просмотр услуг
- ✅ Информация о тренерах 
- ✅ Выбор и покупка абонементов
- ✅ Корзина покупок
- ✅ Оформление заказов 
- ✅ История заказов
- ✅ Личный кабинет 

### Для администраторов

- ✅ Управление пользователями (CRUD)
- ✅ Управление услугами 
- ✅ Управление тренерами
- ✅ Управление абонементами
- ✅ Просмотр и управление заказами
- ✅ Редактирование расписания
- ✅ Изменение статусов заказов и оплаты

## Технологический стек

### Backend
- **PHP 7.4+** 
- **MySQL 5.7+** 
- **PDO**

### Frontend
- **HTML5**
- **CSS3** 
- **JavaScript** 
- **Bootstrap 5** 
- **Font Awesome** 

### Инфраструктура
- **phpMyAdmin** (опционально) — управление БД

## Структура проекта

```
fitnessss/
├── admin/                      # Административная панель
│   ├── includes/              # Общие компоненты админки
│   │   ├── header.php
│   │   └── footer.php
│   ├── index.php              # Главная страница админки
│   ├── users.php              # Управление пользователями
│   ├── user_edit.php          # Редактирование пользователя
│   ├── services.php           # Управление услугами
│   ├── service_edit.php       # Редактирование услуги
│   ├── trainers.php           # Управление тренерами
│   ├── trainer_edit.php       # Редактирование тренера
│   ├── subscriptions.php      # Управление абонементами
│   ├── subscription_edit.php # Редактирование абонемента
│   ├── orders.php             # Управление заказами
│   └── schedule.php           # Редактирование расписания
│
├── api/                       # API endpoints
│   ├── contact.php            # Обработка контактной формы
│   ├── get_order_details.php  # Получение деталей заказа
│   └── process_payment.php    # Обработка платежей
│
├── assets/                    # Статические ресурсы
│   ├── css/
│   │   └── style.css         # Основные стили
│   └── js/
│       └── main.js           # JavaScript функционал
│
├── config/                    # Конфигурация
│   ├── config.php            # Основная конфигурация
│   └── database.php          # Настройки БД
│
├── database/                  # База данных
│   └── schema.sql            # SQL схема БД
│
├── includes/                  # Общие компоненты
│   ├── header.php            # Шапка сайта
│   └── footer.php            # Подвал сайта
│
├── index.php                  # Главная страница
├── login.php                  # Страница входа
├── register.php               # Страница регистрации
├── logout.php                 # Выход из системы
├── services.php               # Страница услуг
├── trainers.php               # Страница тренеров
├── subscriptions.php          # Страница абонементов
├── cart.php                   # Корзина покупок
├── checkout.php               # Оформление заказа
├── orders.php                 # История заказов
├── profile.php                # Личный кабинет
├── faq.php                    # Часто задаваемые вопросы
└── README.md                  # Документация
```

##  Установка и настройка

### Требования

- PHP 7.4 или выше
- MySQL 5.7 или выше (или MariaDB 10.2+)
- Apache/Nginx с поддержкой PHP
- Расширения PHP:
  - `pdo`
  - `pdo_mysql`
  - `mbstring`
  - `session`

   
### Шаг 1: Настройка базы данных
   
1. Создайте базу данных MySQL:
   
```sql
CREATE DATABASE fitness_center CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```
   
2. Импортируйте схему базы данных:

через phpMyAdmin:
- Откройте phpMyAdmin
- Выберите базу данных `fitness_center`
- Перейдите на вкладку "Импорт"
- Выберите файл `database/schema.sql` и нажмите "Вперед"

### Шаг 2: Настройка конфигурации
   
   Откройте файл `config/database.php` и измените параметры подключения:

   ```php
define('DB_HOST', 'localhost');      // Хост БД
define('DB_NAME', 'fitness_center'); // Имя БД
define('DB_USER', 'root');           // Пользователь БД
define('DB_PASS', '');               // Пароль БД
```

Откройте файл `config/config.php` и при необходимости измените:

```php
define('SITE_URL', 'http://localhost/fitness-center'); // URL вашего сайта
define('ADMIN_EMAIL', 'admin@fitness.ru');             // Email администратора
```

### Шаг 3: Настройка веб-сервера

#### Вариант A: Встроенный PHP сервер (для разработки)

   ```bash
   php -S localhost:8000
   ```

Откройте в браузере: `http://localhost:8000`


### Шаг 4: Проверка прав доступа

Убедитесь, что веб-сервер имеет права на чтение всех файлов проекта.

##  Использование

### Тестовые учетные данные

После установки базы данных доступны следующие тестовые аккаунты:

**Администратор:**
     - Логин: `admin`
     - Пароль: `password`

**Обычный пользователь:**
     - Логин: `user1`
     - Пароль: `password`


### Работа для пользователей

1. **Регистрация**
   - Перейдите на страницу регистрации
   - Заполните форму: имя пользователя, email, пароль, ФИО, телефон
   - Нажмите "Зарегистрироваться"

2. **Вход в систему**
   - Введите логин и пароль
   - После успешного входа вы будете перенаправлены на главную страницу

3. **Просмотр услуг**
   - Перейдите в раздел "Услуги"
   - Просмотрите доступные услуги с ценами и описаниями
   - Добавьте понравившиеся услуги в корзину

4. **Выбор тренера**
   - В разделе "Тренеры" ознакомьтесь с информацией о тренерах
   - Узнайте их специализацию, опыт и расписание

5. **Выбор абонемента**
   - В разделе "Абонементы" выберите подходящий абонемент
   - Добавьте его в корзину

6. **Оформление заказа**
   - Перейдите в корзину
   - Проверьте выбранные товары
   - Нажмите "Оформить заказ"
   - Заполните данные для оплаты
   - Подтвердите заказ

7. **История заказов**
   - В разделе "Мои заказы" просмотрите все ваши заказы
   - Отслеживайте статусы заказов и оплаты

### Работа для администраторов

1. **Вход в админ-панель**
   - Войдите как администратор
   - Перейдите в раздел "Админ-панель" или по адресу `/admin/`

2. **Управление пользователями**
   - Просмотр списка всех пользователей
   - Добавление новых пользователей
   - Редактирование данных пользователей
   - Удаление пользователей

3. **Управление услугами**
   - Создание новых услуг
   - Редактирование существующих услуг (название, описание, цена, длительность)
   - Установка связи услуги с тренером
   - Активация/деактивация услуг

4. **Управление тренерами**
   - Добавление информации о тренерах
   - Редактирование данных тренеров
   - Управление специализациями и расписанием

5. **Управление абонементами**
   - Создание новых типов абонементов
   - Настройка цен, сроков действия и количества посещений
   - Управление включенными услугами

6. **Управление заказами**
   - Просмотр всех заказов
   - Изменение статусов заказов (pending, confirmed, cancelled, completed)
   - Управление статусами оплаты (unpaid, paid, refunded)
   - Просмотр деталей заказов

7. **Редактирование расписания**
   - Настройка расписания работы тренеров
   - Управление расписанием проведения услуг

## База данных

### Структура базы данных

База данных включает следующие таблицы:

| Таблица | Описание |
|---------|----------|
| `users` | Пользователи системы (клиенты и администраторы) |
| `trainers` | Информация о тренерах |
| `services` | Услуги фитнес-центра |
| `subscriptions` | Типы абонементов |
| `orders` | Заказы пользователей |
| `order_items` | Элементы заказов (услуги и абонементы) |
| `user_subscriptions` | Активные абонементы пользователей |

### Схема связей

```
users (1) ──< (N) orders
users (1) ──< (N) user_subscriptions
trainers (1) ──< (N) services
subscriptions (1) ──< (N) user_subscriptions
orders (1) ──< (N) order_items
services ──< order_items (item_type='service')
subscriptions ──< order_items (item_type='subscription')
```

### Основные поля таблиц

**users:**
- `id`, `username`, `email`, `password`, `full_name`, `phone`, `role`, `created_at`

**trainers:**
- `id`, `full_name`, `specialization`, `experience`, `phone`, `email`, `bio`, `schedule`

**services:**
- `id`, `name`, `description`, `price`, `duration`, `category`, `trainer_id`, `schedule`, `max_participants`

**subscriptions:**
- `id`, `name`, `description`, `price`, `duration_days`, `visits_count`, `services_included`

**orders:**
- `id`, `user_id`, `total_amount`, `status`, `payment_status`, `payment_method`, `order_date`

Подробная схема базы данных описана в файле `database/schema.sql`.

##  API

Система предоставляет следующие API endpoints:

### `POST /api/process_payment.php`

Обработка платежа за заказ.

**Параметры:**
- `order_id` (int) — ID заказа
- `full_name` (string) — ФИО плательщика
- `email` (string) — Email
- `phone` (string) — Телефон
- `card_number` (string) — Номер карты
- `card_month` (string) — Месяц
- `card_year` (string) — Год
- `card_cvc` (string) — CVC код

**Ответ:**
```json
{
  "success": true,
  "message": "Платеж успешно обработан!",
  "order_id": 1,
  "total_amount": "5000.00"
}
```

### `GET /api/get_order_details.php`

Получение деталей заказа.

**Параметры:**
- `order_id` (int) — ID заказа

**Ответ:**
```json
{
  "success": true,
  "order": { ... },
  "items": [ ... ]
}
```

### `POST /api/contact.php`

Обработка контактной формы.

**Параметры:**
- `name` (string) — Имя
- `email` (string) — Email
- `message` (string) — Сообщение

## Безопасность

В проекте реализованы следующие меры безопасности:

### Защита от SQL-инъекций
- Использование **prepared statements** (PDO) для всех запросов к БД
- Параметризованные запросы вместо конкатенации строк

### Защита от XSS
- Функция `sanitize()` для экранирования вывода
- Использование `htmlspecialchars()` для всех пользовательских данных

### Безопасность паролей
- Хеширование паролей с помощью `password_hash()` (bcrypt)
- Проверка паролей через `password_verify()`

### Контроль доступа
- Проверка авторизации через `requireLogin()`
- Проверка прав администратора через `requireAdmin()`
- Защита админ-панели от неавторизованного доступа

### Валидация данных
- Валидация на стороне сервера для всех форм
- Проверка типов данных и форматов (email, телефон и т.д.)
- Санитизация всех входных данных

### Сессии
- Безопасное управление сессиями
- Проверка прав доступа на каждой защищенной странице

##  Разработка

### Расширение функционала

Для добавления новых функций:

1. Создайте необходимые файлы в соответствующих директориях
2. Обновите схему БД при необходимости
3. Добавьте маршруты и контроллеры
4. Обновите навигацию в `includes/header.php`

##  Лицензия

Этот проект создан в учебных целях.

## Вклад в проект

Для вопросов и предложений обращайтесь к нам на лекции по тису.

---

**Версия:** 1.0  
**Последнее обновление:** 23.11.2025😍😎🥰😘🤩😪😁🚀🚀🚀
